import { ReturnButton, SwitchThemeButton, SearchBar } from "../../components"
import Container from "./styles"

export default () => (
  <Container>
    <ReturnButton />
    <SearchBar />
    <SwitchThemeButton />
  </Container>
)
